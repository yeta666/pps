<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yeta.pps.mapper.MyIncomeExpensesMapper" >

    <insert id="add" parameterType="com.yeta.pps.vo.IncomeExpensesVo">
        insert into income_expenses_${storeId} (id, name, debit_credit, check_item, type) values (#{id}, #{name}, #{debitCredit}, #{checkItem}, #{type});
    </insert>

    <delete id="delete" parameterType="com.yeta.pps.vo.IncomeExpensesVo">
        delete from income_expenses_${storeId} where id = #{id};
    </delete>

    <update id="update" parameterType="com.yeta.pps.vo.IncomeExpensesVo">
        update income_expenses_${storeId} set name = #{name}, debit_credit = #{debitCredit}, check_item = #{checkItem}, type = #{type} where id = #{id};
    </update>

    <resultMap id="incomeExpensesMap" type="com.yeta.pps.po.IncomeExpenses">
        <id property="id" column="id" />
        <result property="name" column="name" />
        <result property="checkItem" column="check_item" />
        <result property="debitCredit" column="debit_credit" />
        <result property="type" column="type" />
    </resultMap>

    <select id="findCount" parameterType="com.yeta.pps.vo.IncomeExpensesVo" resultType="java.lang.Integer">
        select COUNT(0) from income_expenses_${storeId};
    </select>

    <select id="findAll" parameterType="com.yeta.pps.vo.IncomeExpensesVo" resultMap="incomeExpensesMap">
        select
          id, name, debit_credit, check_item, type
        from
          income_expenses_${storeId}
        where
          1 = 1
        <if test="type!=null">
            and type = #{type}
        </if>
    </select>

    <select id="findAllPaged" resultMap="incomeExpensesMap">
        select id, name, debit_credit, check_item, type from income_expenses_${incomeExpensesVo.storeId} limit #{pageVo.page}, #{pageVo.pageSize};
    </select>

    <select id="findById" parameterType="com.yeta.pps.vo.IncomeExpensesVo" resultMap="incomeExpensesMap">
        select id, name, debit_credit, check_item, type from income_expenses_${storeId} where id = #{id};
    </select>
    
</mapper>