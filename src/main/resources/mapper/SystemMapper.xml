<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yeta.pps.mapper.SystemMapper" >

    <select id="findSystem" resultType="com.yeta.pps.po.System">
        select push_money_rate from system where id = 1;
    </select>

    <update id="updatePushMoneyRate" parameterType="com.yeta.pps.po.System">
        update system set push_money_rate = #{pushMoneyRate} where id = 1 and #{pushMoneyRate} &gt;= 0;
    </update>

</mapper>