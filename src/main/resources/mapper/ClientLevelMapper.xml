<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yeta.pps.mapper.ClientLevelMapper" >

    <update id="update" parameterType="com.yeta.pps.po.ClientLevel">
        update client_level set name = #{name}, price_type = #{priceType}, price = #{price} where id = #{id};
    </update>

    <select id="findCount" parameterType="com.yeta.pps.po.ClientLevel" resultType="java.lang.Integer">
        select count(0) from client_level;
    </select>

    <select id="findAllPaged" parameterType="com.yeta.pps.vo.PageVo" resultType="com.yeta.pps.po.ClientLevel">
        select id, name, price_type, price from client_level limit #{start}, #{pageSize};
    </select>

</mapper>